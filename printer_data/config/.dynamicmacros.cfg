[gcode_macro UPDATE_MAINSAIL]
gcode = {% set _ = python_file('mainsail.py') %}
	DYNAMIC_MACRO MACRO=UPDATE_MAINSAIL {rawparams}

[gcode_macro MMU_ENDLESS_SPOOL_SHOW_GROUPS]
gcode = 
	DYNAMIC_MACRO MACRO=MMU_ENDLESS_SPOOL_SHOW_GROUPS {rawparams}

[gcode_macro MMU_ENDLESS_SPOOL_RESET]
gcode = 
	DYNAMIC_MACRO MACRO=MMU_ENDLESS_SPOOL_RESET {rawparams}

